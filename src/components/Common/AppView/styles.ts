import {StyleSheet} from 'react-native';

const createAppViewStyles = () => {
  return StyleSheet.create({
    container: {
      flex: 1,
      paddingHorizontal: 0,
    },
  });
};
export {createAppViewStyles};
