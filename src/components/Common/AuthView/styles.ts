import {StyleSheet} from 'react-native';

const createAppViewStyles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 10,
    paddingHorizontal: 16,
  },
});

export {createAppViewStyles};
